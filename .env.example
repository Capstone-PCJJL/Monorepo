# =============================================================================
# Monorepo Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# See README.md for detailed instructions on how to get each value.
# =============================================================================

# =============================================================================
# TMDB API Configuration (Backend)
# =============================================================================
# Get credentials from: https://www.themoviedb.org/settings/api
# 1. Create account at themoviedb.org
# 2. Go to Settings > API
# 3. Click Create > Developer > Accept terms
# 4. Copy both keys below

API_KEY=your_tmdb_api_key_here
TMDB_BEARER_TOKEN=your_tmdb_bearer_token_here
BASE_URL=https://api.themoviedb.org/3

# =============================================================================
# MySQL Database Configuration
# =============================================================================
# DB_MODE is set automatically by the Makefile commands:
#   make up-local  -> Sets DB_MODE=local (Docker MySQL)
#   make up-remote -> Sets DB_MODE=remote (AWS RDS)
# =============================================================================

# --- Local Docker Database (for development) ---
# Command: make up-local
LOCAL_SQL_HOST=localhost
LOCAL_SQL_PORT=3306
LOCAL_SQL_USER=root
LOCAL_SQL_PASS=password
LOCAL_SQL_DB=tmdb

# --- Remote AWS RDS (for production/staging) ---
# Command: make up-remote
REMOTE_SQL_HOST=your-rds-endpoint.region.rds.amazonaws.com
REMOTE_SQL_PORT=3306
REMOTE_SQL_USER=your_rds_username
REMOTE_SQL_PASS=your_rds_password
REMOTE_SQL_DB=tmdb

# =============================================================================
# Backend API Server Configuration
# =============================================================================
# API_HOST: Use 0.0.0.0 for Docker, 127.0.0.1 for local development
# ALLOWED_ORIGINS: Comma-separated list of frontend URLs for CORS

API_HOST=0.0.0.0
API_PORT=8000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# =============================================================================
# Frontend Configuration (Vite)
# =============================================================================
# API_PROXY_TARGET: Backend URL for Vite dev server proxy
#   Docker: http://backend:8000 (uses docker-compose service name)
#   Local:  http://localhost:8000

API_PROXY_TARGET=http://localhost:8000

# =============================================================================
# Firebase Authentication (Frontend)
# =============================================================================
# Get credentials from: https://console.firebase.google.com/
# 1. Select your project (or create one)
# 2. Click gear icon > Project Settings
# 3. Scroll to "Your apps" > Web app (add one if needed)
# 4. Copy values from the firebaseConfig object

VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id
